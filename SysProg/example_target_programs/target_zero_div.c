#include <stdio.h>

int main() {
    int dividend;
    int divisor = 0;
    int result;

    scanf("%d", &dividend);

    result = dividend / divisor;

    printf("Result: %d\n", result);

    return 0;
}
